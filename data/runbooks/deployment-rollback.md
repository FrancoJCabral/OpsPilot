# Deployment rollback

When failures begin immediately after deployment, stop the rollout and compare error rate, latency, configuration, and migrations with the prior release. Roll back to the last healthy artifact if data compatibility permits. Record the deployment ID, timestamps, logs, and validation results.

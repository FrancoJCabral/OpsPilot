# Database latency

For slow database requests, inspect connection-pool saturation, active locks, query duration, CPU, storage latency, and recent schema changes. Identify the slow query from traces, verify its execution plan and indexes, and reduce load before increasing pool limits.
